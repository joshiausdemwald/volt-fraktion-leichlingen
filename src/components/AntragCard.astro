---
import MotionCard from './MotionCard.astro';

interface Props {
  title: string;
  date: Date;
  status: 'eingereicht' | 'beschlossen' | 'abgelehnt' | 'in_beratung' | 'ersetzt';
  motionId: string;
  summary: string;
  slug: string;
  documentUrl?: string;
}
---

<MotionCard {...Astro.props} />
